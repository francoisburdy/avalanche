'use strict';

describe('myApp.parametres module', function() {

  beforeEach(module('myApp.parametres'));

  describe('parametres controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var parametresCtrl = $controller('ParametresCtrl');
      expect(parametresCtrl).toBeDefined();
    }));

  });
});
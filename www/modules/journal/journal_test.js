'use strict';

describe('myApp.suivi module', function() {

  beforeEach(module('myApp.suivi'));

  describe('suivi controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var view1Ctrl = $controller('View1Ctrl');
      expect(view1Ctrl).toBeDefined();
    }));

  });
});